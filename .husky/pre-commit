#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Build the shared eslint config package before linting staged files
pnpm run build:eslint-config

# Run lint-staged: lint + format staged files only
pnpm exec lint-staged
