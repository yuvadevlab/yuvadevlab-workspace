# 📚 Internal Libraries (`libs/`)

This directory contains internal, non-publishable domain models and shared infrastructure adapters.

---

## 🧭 Subdirectories

- **`domain/`**: Enterprise entities, value objects, and pure domain interface definitions.
- **`infrastructure/`**: Database repositories, ORM mappers, and internal utility adapters.
